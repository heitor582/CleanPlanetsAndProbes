postgresql:
  username: ${POSTGRESQL_USERNAME:planets_probe}
  password: ${POSTGRESQL_PASSWORD:123456}
  schema: ${POSTGRESQL_SCHEMA:planets_probe}
  url: ${POSTGRESQL_URL:localhost:5432}

spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none